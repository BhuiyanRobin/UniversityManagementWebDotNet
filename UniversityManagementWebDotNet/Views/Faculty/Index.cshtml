@model IEnumerable<UniversityManagementWebDotNet.Models.DAO.Faculty>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FacultyName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FacultyCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FacultyEmail)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FacultyContactNumber)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FacultyName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FacultyCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FacultyEmail)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FacultyContactNumber)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
